# Optional — convenient for local container testing.
# Usage: docker compose up --build
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=http://localhost:3000
      - EMAIL_PROVIDER=none
    restart: unless-stopped
