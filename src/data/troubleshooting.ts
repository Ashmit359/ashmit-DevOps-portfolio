export interface TroubleshootingCase {
  id: string;
  title: string;
  category: string;
  symptoms: string;
  check: string;
  command: string;
  possibleCause: string;
  rootCause: string;
  fix: string;
  prevention: string;
}

export const troubleshootingCases: TroubleshootingCase[] = [
  {
    id: "ec2-ssh-refused",
    title: "EC2 SSH Connection Refused",
    category: "EC2",
    symptoms: "SSH client reports 'connection refused' when connecting to an instance.",
    check: "Verify the security group allows inbound SSH from your IP, and that the instance is running.",
    command: "aws ec2 describe-instances --instance-ids <id>",
    possibleCause: "Security group missing an inbound rule for port 22, or the SSH daemon isn't running.",
    rootCause: "Most often a security group rule scoped to the wrong CIDR range.",
    fix: "Add or correct the inbound rule to allow SSH from the required source.",
    prevention: "Use a bastion host or SSM Session Manager instead of open SSH ports.",
  },
  {
    id: "ec2-high-cpu",
    title: "EC2 High CPU",
    category: "EC2",
    symptoms: "CloudWatch shows sustained high CPU utilization; the app feels slow.",
    check: "Look at what process is consuming CPU on the instance.",
    command: "top -o %CPU",
    possibleCause: "A runaway process, an inefficient query, or genuinely increased load.",
    rootCause: "Varies — confirm with process-level metrics before assuming it's load.",
    fix: "Scale horizontally, optimize the offending process, or right-size the instance.",
    prevention: "Set CloudWatch alarms on CPU with headroom before it becomes user-visible.",
  },
  {
    id: "disk-full",
    title: "Disk Full",
    category: "Linux",
    symptoms: "Application writes start failing; system logs show 'No space left on device'.",
    check: "Check disk usage per filesystem and per directory.",
    command: "df -h && du -sh /var/log/*",
    possibleCause: "Unrotated logs, old build artifacts, or a runaway temp file.",
    rootCause: "Log rotation not configured or a process writing unbounded data.",
    fix: "Clear or archive the offending files and free space immediately.",
    prevention: "Configure logrotate and disk-usage alarms before it's an emergency.",
  },
  {
    id: "memory-high",
    title: "High Memory Usage",
    category: "Linux",
    symptoms: "System becomes slow or the OOM killer terminates processes.",
    check: "Identify the top memory-consuming processes.",
    command: "free -m && ps aux --sort=-%mem | head",
    possibleCause: "A memory leak, an undersized instance, or a spike in concurrent load.",
    rootCause: "Confirm whether usage grows over time (leak) or spikes with traffic (sizing).",
    fix: "Restart the affected service, then fix the leak or resize the instance.",
    prevention: "Track memory trend over time, not just point-in-time snapshots.",
  },
  {
    id: "alb-503",
    title: "ALB Returning 503",
    category: "AWS",
    symptoms: "Clients receive HTTP 503 from the load balancer.",
    check: "Check target group health and target response codes.",
    command: "aws elbv2 describe-target-health --target-group-arn <arn>",
    possibleCause: "No healthy targets registered, or all targets failing health checks.",
    rootCause: "Application not listening on the expected port/path, or it's overwhelmed.",
    fix: "Fix the health check path or scale up healthy capacity.",
    prevention: "Alert on target health count dropping below a safe minimum.",
  },
  {
    id: "alb-unhealthy-target",
    title: "ALB Unhealthy Target",
    category: "AWS",
    symptoms: "One or more targets show as unhealthy in the target group.",
    check: "Manually curl the health check path from within the VPC.",
    command: "curl -i http://<target-ip>:<port>/health",
    possibleCause: "Wrong health check path/port, or the app is slow to start.",
    rootCause: "Mismatch between the configured health check and what the app actually serves.",
    fix: "Align the health check settings with the application's real health endpoint.",
    prevention: "Test health checks as part of the deployment pipeline.",
  },
  {
    id: "iam-access-denied",
    title: "IAM AccessDenied",
    category: "AWS",
    symptoms: "An API call fails with an AccessDenied error.",
    check: "Read the exact action and resource in the denial message.",
    command: "aws sts get-caller-identity",
    possibleCause: "The calling role/user's policy doesn't grant the specific action.",
    rootCause: "Overly narrow (or missing) IAM policy statement for the action being called.",
    fix: "Add the minimum required permission, scoped to the specific resource.",
    prevention: "Use IAM Access Analyzer and policy simulation before deploying new roles.",
  },
  {
    id: "s3-access-denied",
    title: "S3 AccessDenied",
    category: "AWS",
    symptoms: "Requests to an S3 object or bucket return AccessDenied.",
    check: "Check both the IAM policy and the bucket policy — either can deny.",
    command: "aws s3api get-bucket-policy --bucket <bucket>",
    possibleCause: "Bucket policy explicitly denies, or Block Public Access is preventing intended access.",
    rootCause: "Conflicting policies between IAM and the bucket resource policy.",
    fix: "Reconcile the two policies so the intended access path is explicitly allowed.",
    prevention: "Keep one clear owner for bucket policy changes to avoid drift.",
  },
  {
    id: "waf-blocking-traffic",
    title: "WAF Blocking Legitimate Traffic",
    category: "AWS",
    symptoms: "Valid requests are blocked with a WAF-generated 403.",
    check: "Review WAF sampled requests to see which rule matched.",
    command: "aws wafv2 get-sampled-requests --web-acl-arn <arn> --rule-metric-name <rule>",
    possibleCause: "An overly broad managed rule or a custom rule with a false-positive pattern.",
    rootCause: "Rule tuned for generic threats, not this application's real traffic shape.",
    fix: "Add a targeted exception or switch the rule to count mode while tuning.",
    prevention: "Roll out new WAF rules in count mode before switching to block.",
  },
  {
    id: "k8s-crashloopbackoff",
    title: "Kubernetes CrashLoopBackOff",
    category: "Kubernetes",
    symptoms: "A pod repeatedly starts and crashes.",
    check: "Read the container's logs and previous-container logs.",
    command: "kubectl logs <pod> --previous",
    possibleCause: "Application error on startup, missing config, or a failing dependency.",
    rootCause: "Confirmed from the actual error in the crash logs, not assumed.",
    fix: "Fix the underlying application/config error, then let Kubernetes restart it.",
    prevention: "Add startup probes so failures surface clearly and fast.",
  },
  {
    id: "k8s-imagepullbackoff",
    title: "Kubernetes ImagePullBackOff",
    category: "Kubernetes",
    symptoms: "A pod is stuck unable to pull its container image.",
    check: "Describe the pod to see the exact pull error.",
    command: "kubectl describe pod <pod>",
    possibleCause: "Wrong image tag, private registry auth missing, or a typo in the image name.",
    rootCause: "Usually a missing imagePullSecret or an incorrect tag.",
    fix: "Correct the image reference or add the required pull secret.",
    prevention: "Validate image references as part of the CI pipeline before deploy.",
  },
  {
    id: "pod-pending",
    title: "Pod Stuck Pending",
    category: "Kubernetes",
    symptoms: "A pod stays in Pending and never schedules.",
    check: "Check scheduler events for the pod.",
    command: "kubectl describe pod <pod>",
    possibleCause: "Insufficient cluster resources, or unmet node affinity/taint requirements.",
    rootCause: "Confirmed from the scheduling failure reason in pod events.",
    fix: "Free up resources, adjust requests, or relax the scheduling constraint.",
    prevention: "Monitor cluster resource headroom before it blocks new pods.",
  },
  {
    id: "container-creating",
    title: "Pod Stuck in ContainerCreating",
    category: "Kubernetes",
    symptoms: "A pod stays in ContainerCreating for an extended time.",
    check: "Look at pod events for volume mount or network setup errors.",
    command: "kubectl describe pod <pod>",
    possibleCause: "A PVC that can't bind, or a CNI networking issue.",
    rootCause: "Usually a storage or networking dependency that isn't ready.",
    fix: "Resolve the underlying volume or network provisioning issue.",
    prevention: "Validate storage classes and CNI health as part of cluster checks.",
  },
  {
    id: "oomkilled",
    title: "Pod OOMKilled",
    category: "Kubernetes",
    symptoms: "A container is killed and restarted with reason OOMKilled.",
    check: "Compare the container's memory usage against its configured limit.",
    command: "kubectl top pod <pod>",
    possibleCause: "Memory limit set too low, or an actual leak in the application.",
    rootCause: "Confirmed by watching memory trend leading up to the kill.",
    fix: "Raise the memory limit appropriately or fix the leak.",
    prevention: "Set requests/limits based on observed usage, not guesswork.",
  },
  {
    id: "service-unavailable",
    title: "Service Unavailable",
    category: "Kubernetes",
    symptoms: "Requests to a Service time out or are refused.",
    check: "Confirm the Service's selector matches labels on running, ready pods.",
    command: "kubectl get endpoints <service>",
    possibleCause: "Label selector mismatch, or no pods currently passing readiness checks.",
    rootCause: "Usually a selector/label mismatch introduced by a manifest change.",
    fix: "Align the Service selector with the pod template's labels.",
    prevention: "Lint manifests for selector/label consistency before applying.",
  },
  {
    id: "ingress-failure",
    title: "Ingress Not Routing Traffic",
    category: "Kubernetes",
    symptoms: "Requests to the Ingress host return errors or 404s.",
    check: "Confirm the Ingress controller is running and the Ingress resource is valid.",
    command: "kubectl describe ingress <name>",
    possibleCause: "Missing ingress class, wrong backend service name, or controller not installed.",
    rootCause: "Confirmed from the Ingress controller's own logs and events.",
    fix: "Correct the Ingress spec or install/fix the controller.",
    prevention: "Test Ingress rules in a staging namespace before production rollout.",
  },
  {
    id: "dns-failure",
    title: "DNS Resolution Failure",
    category: "Networking",
    symptoms: "A service or pod can't resolve a hostname it depends on.",
    check: "Test DNS resolution directly from within the cluster or host.",
    command: "dig <hostname>",
    possibleCause: "CoreDNS misconfiguration, or an external DNS record issue.",
    rootCause: "Confirmed by checking whether resolution fails everywhere or just from one context.",
    fix: "Fix the DNS record or restart/reconfigure the resolver.",
    prevention: "Add DNS resolution checks to routine health monitoring.",
  },
];

export const troubleshootingCategories = Array.from(
  new Set(troubleshootingCases.map((c) => c.category))
);
